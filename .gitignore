# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OAuth credentials & tokens — NEVER commit
credentials.json
token.json
client_secret*.json
.env
.env.*
!.env.example

# Local app state — token.json, state.db, future caches. Never committed.
data/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Claude Code agent worktrees (runtime; not source)
.claude/worktrees/

# td local store (per-project, not portable)
.todos/
.sidecar/
.sidecar-agent
.sidecar-task
.sidecar-pr
.sidecar-start.sh
.sidecar-base
.td-root
